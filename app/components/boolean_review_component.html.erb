<div class="inline-flex flex-col items-center rounded-lg border border-purple-500 p-4 mb-4 text-center">
  <h3 class="font-semibold text-gray-800 mb-1"><%= review_name %></h3>

  <span class="text-xl font-extrabold text-indigo-900 mb-3"><%= display_rating %></span>

  <h4 class="text-xs font-semibold text-indigo-900 mb-1">Tu puntuación</h4>
  <div class="flex items-center justify-center gap-4">
    <% [true, false].each do |value| %>
      <div class="flex items-center gap-1">
        <%= vote_button(value) %>
        <span class="text-xs text-gray-500"><%= value ? likes_count : dislikes_count %></span>
      </div>
    <% end %>
  </div>
</div>
