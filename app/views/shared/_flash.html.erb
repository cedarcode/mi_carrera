<% flash.each do |type, message| %>
  <div class="p-4 mb-4 rounded-lg <%= type == "alert" ? "bg-red-100 text-red-800" : "bg-blue-100 text-blue-800" %>">
    <span class="ms-3 text-sm/6 font-medium"><%= message %></span>
  </div>
<% end %>
