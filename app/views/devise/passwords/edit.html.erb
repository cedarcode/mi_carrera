<div class="mdc-deprecated-list-group">
  <h3 class="mdc-deprecated-list-group__subheader mdc-typography--headline5 sign-in">Restablecer Contraseña</h3>
  <ul class="mdc-deprecated-list growing-text-list">

    <%= form_with(model: resource, url: password_path(resource_name), method: :put, local: true, class: "space-y-6") do |f| %>
      <%= f.hidden_field :reset_password_token %>

      <%= render InputFieldComponent.new( form: f, attribute: :password, label: "Nueva contraseña", type: :password, required: true) do |component| %>
        <% component.with_trailing_icon do %>
          <%= render "shared/password_visibility_button" %>
        <% end %>

        <span class="text-xs text-gray-500">
          La contraseña debe tener al menos 6 caracteres
        </span>
      <% end %>

      <li class="form-input-container sign-in">
        <%= f.button class: "mdc-button mdc-button--raised" do %>
          <span class="mdc-button__ripple"></span>
          <span class='mdc-button__label'>
            Restablecer contraseña
          </span>
        <% end %>
      </li>
    <% end %>

    <%= render "devise/shared/links" %>
  </ul>
</div>
