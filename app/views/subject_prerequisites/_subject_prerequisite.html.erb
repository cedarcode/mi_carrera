<%= link_to subject_path(subject_prerequisite.approvable_needed.subject), class: 'flex items-center py-3 px-4 hover:bg-gray-100 cursor-pointer' do %>
  <span class="flex-1">
    <%= "No haber aprobado " if negative %>
    <%= display_subject_prerequisite(subject_prerequisite) %>
  </span>

  <%= render 'prerequisites/met_checkmark', prerequisite: subject_prerequisite, negative: %>
<% end %>
