<div class="px-4 py-4 flex flex-col h-[calc(100dvh-3.5rem)] sm:h-[calc(100dvh-4rem)]">
  <div class="flex items-center justify-between mb-4">
    <h1 class="text-xl font-semibold text-gray-900">Grafo de malla curricular sugerida</h1>
  </div>

  <% if @subjects.any? %>
    <%= render "shared/graph_legend" %>

    <p class="mb-2 text-xs text-gray-500">
      <span class="hidden sm:inline">Click en una materia para ver sus previas y dependencias. Doble click para ver detalles.</span>
      <span class="sm:hidden">Toca una materia para ver sus previas. Toca de nuevo para ver detalles.</span>
    </p>

    <%= render SubjectGraphComponent.new(
      subjects: @subjects,
      current_student: current_student
    ) %>
  <% else %>
    <div class="flex flex-col justify-center items-center mt-12 text-gray-500">
      <p class="text-lg">No hay materias para mostrar.</p>
    </div>
  <% end %>
</div>
