language: ruby
services:
  - postgresql
before_install:
  - nvm install
  - nvm use
before_script:
  - bundle install --jobs=3 --retry=3
  - yarn
  - bundle exec rake db:create
  - bundle exec rake db:schema:load
